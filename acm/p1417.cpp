#include <bits/stdc++.h>
using namespace std;
const int maxn = 1000010;
int A[maxn],B[maxn],C[maxn],dp[maxn];
int main(){
    int t,n;
    scanf("%d%d",&t,&n);
    for(int i = 1;i <= n;i++){
        scanf("%d",&A[i]);
    }
    for(int i = 1;i <= n;i++){
        scanf("%d",&B[i]);
    }
    for(int i = 1;i <= n;i++){
        scanf("%d",&B[i]);
    }

    for(int i = 1;i <= n;i++)
        for(int j = 1;j <= n;j++)
            for(int k = 1)

    return 0;
}
